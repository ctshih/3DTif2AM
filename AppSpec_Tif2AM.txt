我有一個 3D Tif 檔 xxxxx.tif，我要把它轉換一個 AmiraMesh 3D ASCII 2.0，請寫一個程式來做這件事。規格如下：

1. 使用 Python 語言
2. 利用 localhost 透過瀏覽器開圖形介面
3. 介面元素包括：
  a. 檔案選擇：開啟 3D tif 所在的目錄（預設論竟為開啟此程式之目錄位置），使用者選取所要用到的 tif 檔案。
  b. Voxel size 在 x, y, z 三個方向的長度輸入框。
  c. 「轉檔」按鈕
4. 動作：
  a. 當使用者選好檔案後，顯示檔案名稱、影像尺寸、voxel size，要求使用者確認
  b. 當使用者按下「轉檔」按鈕後，跳出檔案選擇視窗，讓使用者選擇要輸出的路徑與檔名，路徑預設為 tif 資料檔所在位置，檔名為 xxxxx-3d-ascii.am，並要求使用者「確認」或「取消」
  c. 當使用者按下確認鍵後，轉檔並輸出